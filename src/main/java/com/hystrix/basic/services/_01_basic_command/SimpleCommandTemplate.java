package com.hystrix.basic.services._01_basic_command;

import com.hystrix.basic.services.RemoteService;
import com.netflix.hystrix.HystrixCommand;
import com.netflix.hystrix.HystrixCommandGroupKey;
import org.apache.log4j.Logger;

/**
 * User: Szymon Mezglewski
 */
public class SimpleCommandTemplate {
    private static final Logger LOG = Logger.getLogger(SimpleCommandTemplate.class);

}
